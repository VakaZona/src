{% extends 'base.html.twig' %}

{% block title %}Games {% endblock %}

{% block body %}
    <h2>Games</h2>

    {% for game in games %}
        <h4>{{ game }}</h4>
        <p>
            <a href="{{ path('game', {'slug': game.slug}) }}">View</a>
        </p>
    {% endfor %}
{% endblock %}